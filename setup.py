from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Learn the downstream tasks using Transformer models.',
    author='Pawan Niroula',
    license='MIT',
)
